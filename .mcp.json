{
  "mcpServers": {
    "training-log": {
      "type": "http",
      "url": "${TRAINING_LOG_MCP_URL:-https://training-log-bot.fly.dev/mcp}",
      "headers": {
        "Authorization": "Bearer ${TRAINING_LOG_MCP_TOKEN}"
      }
    }
  }
}
