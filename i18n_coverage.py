"""Реестр локализации: какие модули уже переведены, какие никогда не будут, и
что ещё не решено. Это храповик для `tests/test_i18n_no_leaks.py`, а не просто
документация — сам файл не запрещает и не разрешает ничего, запрет утечек
живёт в тесте, здесь только источник правды о том, к какому модулю какое
правило применять.

Почему это важно как отдельный модуль, а не комментарий в тесте: список
`LOCALIZED` обязан РАСТИ по мере перевода экранов, и разработчик, переводящий
следующий модуль, должен одной строкой сюда его добавить — тест тут же начнёт
запрещать в нём кириллицу навсегда. Без реестра эта защита существовала бы
только «пока помним».

Три списка обязаны в сумме покрывать каждый .py-файл из ROOTS (см. ниже) ровно
один раз — за этим следит test_every_module_is_classified в тесте: новый файл,
про который никто не принял решение "переводим/не переводим", не должен
проскочить молча.

Область действия (ROOTS) — только продуктовый код (корень репозитория и
handlers/): тесты и scripts/ — это инструменты разработки, а не то, что видит
пользователь бота, поэтому в реестр их сознательно не включаем.
"""

from __future__ import annotations

import ast
from pathlib import Path

import exercise_descriptions

_ROOT = Path(__file__).resolve().parent

# Каталоги, из которых модули должны быть классифицированы. Тесты (tests/) и
# служебные скрипты (scripts/) сюда не входят: это код для разработчика, а не
# для атлета, и локализация продукта его не касается.
ROOTS: tuple[Path, ...] = (_ROOT, _ROOT / "handlers")


def discover_modules() -> list[str]:
    """Список модулей репозитория в области действия реестра — относительные
    пути со слэшем (`handlers/admin.py`), как они записаны в списках ниже.
    Рекурсии внутри ROOTS нет специально: `handlers/` — плоская директория, а
    новый вложенный пакет — это уже решение, которое стоит заметить руками, а
    не проглотить рекурсивным glob'ом.
    """
    modules: list[str] = []
    for root in ROOTS:
        for path in sorted(root.glob("*.py")):
            rel = path.relative_to(_ROOT).as_posix()
            modules.append(rel)
    return modules


# --- LOCALIZED --------------------------------------------------------------
#
# Модули, в которых ЦЕЛИКОМ (весь файл, а не отдельная функция) нет ни одного
# кириллического строкового литерала вне докстрингов/комментариев. Список
# только растёт: попадание модуля сюда — обещание, что новый русский текст в
# нём — баг, а не "ещё не успели".
#
# keyboards.py — первый файл в списке: все подписи инлайн- и reply-кнопок
# (главное меню, тренировка, упражнения, программы, дневник еды, дневник веса,
# настройки, MCP, история, календарь, импорт CSV и т.д.) идут через i18n.t()/
# _lazy()/_ReplyButtonMatch, единственное сознательное исключение — автоним
# «Русский» в LANG_NAMES (не переводится нигде, см. TONE_OF_VOICE.md).
# handlers/settings.py остаётся в TODO: экран настроек, который строит
# keyboards.settings_keyboard, переведён целиком, но handlers/settings.py —
# это ещё и алерты после смены единиц/формулы («Перевёл всё на …») и другие
# сообщения хендлера, которых этот проход не касался.
#
# handlers/workout.py, handlers/edit_workout.py, handlers/bodyweight.py —
# запись тренировки целиком: живой трекер, справка (/help), онбординг
# новичка (_ONBOARDING/_GREETING), правка прошлой тренировки, дневник веса.
# Заодно закрыта протечка formatting.UNIT_LABELS (см. formatting.unit_label) —
# англоязычный с килограммами раньше видел «кг» в тексте и на графике веса
# тела (charts.render_metric_over_sessions).
#
# handlers/history.py, handlers/food_diary.py, handlers/csv_import.py,
# handlers/sharing.py — история и прогресс, дневник питания, импорт CSV и
# шаринг программ/упражнений. Заодно найден и закрыт настоящий баг с языком:
# handlers/csv_import.py._parse_row_date узнавал "дата ещё в будущем" по
# русской подстроке в тексте исключения — на английском каталоге parser.py
# отдаёт этот же текст по-английски, и проверка молча ломалась (см.
# _parse_row_date). Заголовок графика прогресса
# (handlers/history.py._render_progress_view, метрика "повторы"/"reps") —
# текст, уезжающий в пиксели matplotlib'а, а не в каталог сообщений, поэтому
# его легко было пропустить при поиске по строкам чата.
#
# handlers/exercises.py, handlers/routines.py, handlers/exercise_resolve.py —
# карточка/CRUD упражнений и групп мышц, экран программ (готовые/AI/из
# тренировки), резолв незнакомых имён при импорте CSV. Заодно закрыты две
# протечки каталожных (is_template=1) названий: шаблон показывал канонический
# русский `name`/`display_name` вместо локализованного (см.
# handlers.exercises._template_display_name и то же в
# handlers.routines._rtadd_catalog_screen через
# `seed_data.localized_exercise_name`) — этот инвариант («original_name/каталог
# всегда по-русски, экран — на языке пользователя») держит связь с
# free-exercise-db и раньше был нарушен именно на шаблонах, ещё не форкнутых
# пользователю. Восемь безличных «Программа не найдена»/«Тренировка не
# найдена» в handlers/routines.py и одно «Шаблон не найден» в
# handlers/exercises.py приведены к правилу «что случилось + что делать
# дальше» (см. ui.alert_exercise_not_found — тот же приём, уже был в ui.py).
# Готовые каталожные программы (seed_data.WORKOUT_PROGRAMS/PROGRAM_BY_KEY:
# названия дней, meta, описания, схемы подходов) остаются русскими — это
# данные каталога, а не текст хендлера, и их перевод не входит в этот проход.
#
# main.py, ai_limits.py, handlers/settings.py, handlers/fallback.py,
# handlers/persistent_menu.py, handlers/feedback.py, handlers/donate.py,
# handlers/community.py, handlers/game.py, handlers/backfill.py,
# handlers/mcp_access.py — оставшиеся мелкие хендлеры и последний рубеж
# (main.on_unhandled_error). Заодно закрыт долг из TONE_OF_VOICE.md: экран
# /mcp (handlers/mcp_access.py) был английским независимо от языка бота и
# показывал «Bearer» рядом с «ПРИВЕТ АТЛЕТ» — теперь голос тренера на обоих
# языках, как и everywhere else.
#
# main.py._generic_error_text/_back_to_menu_markup — двойная защита: обычный
# путь берёт текст из каталога (i18n.t), но если сам i18n.t() бросит исключение
# (регресс ICU-парсера, битый каталог — что угодно), обработчик ошибок не
# должен уронить и это тоже — тогда в дело идёт захардкоженный русский
# fallback (см. _FALLBACK_ERROR_TEXT/_FALLBACK_MENU_BUTTON_TEXT), который
# кириллицей не считается багом, а не долгом: он и должен остаться постоянным
# резервным текстом, а не каталожным ключом (иначе он был бы такой же
# точкой отказа, как то, от чего защищает).
#
# ai_limits.py — тексты про исчерпанные квоты (QUESTION_LIMIT_TEXT,
# _hard_stop_text, _video_text, _food_text) идут через каталог на оба языка.
# `QUESTION_LIMIT_TEXT` остаётся модульным атрибутом через `__getattr__`
# (PEP 562) ради `handlers/ai_trainer.py` (его в этом проходе не трогаем),
# который читает `ai_limits.QUESTION_LIMIT_TEXT` как готовую строку, — плоская
# константа заморозила бы её на языке дефолта в момент импорта процесса.
# `preview_text`/`_KIND_TITLES`/строки `Block.log` остаются по-русски навсегда
# — это экран для своих аккаунтов (ADMIN_ID/TEST_USER_ID) и служебные строки
# в лог, а не то, что видит атлет (см. ALLOWED_CYRILLIC ниже).
#
# handlers/backfill.py — тот же приём PEP 562 для `_BACKFILL_PROMPT`:
# `handlers/workout.py` (не наш файл в этом проходе) делает
# `from handlers.backfill import _BACKFILL_PROMPT` внутри функции, и module-
# level `__getattr__` пересчитывает строку в языке текущего запроса на каждое
# обращение вместо однократной заморозки при импорте.
#
# search_terms.py, exercise_mentions.py, program_mentions.py — особый случай,
# описанный в CLAUDE.md (Ratchet): эти модули не показывают текст пользователю
# вообще, они разбирают ЕГО ввод. Вся кириллица в них — суффиксы для стемминга,
# ключи/значения таблиц синонимов и фраз, канонические сравнения с данными БД
# («планка» как омоним-идиома) — то есть ровно то же самое исключение, что уже
# узаконено для handlers/csv_import.py (разбор чужого файла) чуть выше. После
# аллоулиста ниже в них не остаётся ни одного литерала показываемого текста,
# поэтому им можно жить в LOCALIZED по-честному: НОВАЯ кириллица здесь — либо
# ещё один законный элемент разбора (тогда его тоже вносят в ALLOWED_CYRILLIC
# с причиной), либо утечка показываемого текста, и тест обязан её поймать.
#
# handlers/ai_trainer.py, handlers/factcheck.py, running_texts.py — экраны
# AI-тренера (интро, готовые вопросы, лимиты, опросник перед сборкой
# программы, откаты действий, разбор видео/голоса/фото) и разбор пересланного
# поста. Корневой ai_trainer.py (промпты) НЕ входит сюда — языковая директива
# там уже приклеена отдельно (_with_language_tail), это другой слой и другая
# задача. Экранные тексты, которые читаются извне как модульные атрибуты, идут
# через module __getattr__ (PEP 562, тот же приём, что у formatting.py и
# ai_limits.py). Оговорка: он чинит только чтение через `module.ATTR` в момент
# использования — `from ... import CONST` на верхнем уровне связывает ЗНАЧЕНИЕ
# один раз за процесс, и со стороны отдающего модуля это не лечится никак.
# persistent_menu.py раньше делал именно так, и нижняя кнопка «AI-тренер»
# показывала интро на языке по умолчанию; теперь он читает через
# `ai_trainer_handlers.INTRO_TEXT`, и оба входа — нижняя кнопка и инлайн
# (menu:ai) — говорят на языке пользователя. Форма записи закреплена тестом
# (tests/test_no_frozen_language.py): следствие тут поймать нельзя, запрещать
# надо сам приём. Пулы placeholder-фраз
# «тренер думает» (running_texts.POOLS/POOLS_EN и FACT_CHECK_POOL) переехали в
# каталог тем же приёмом, что push_texts.py (ключи `thinking.<тема>.<n>` и
# `factcheck.thinking.<n>`) — раньше английский пул жил рядом с русским прямо
# в модуле, теперь оба каталога проходят те же три слоя test_i18n_no_leaks.py.
LOCALIZED: list[str] = [
    # Модули REST-слоя с человеческим текстом: у достижений есть название и
    # описание, у сводки — заголовок, звание и подписи плиток; и те, и другие
    # рендерятся через i18n.use_lang(users.lang). Остальные api_v1_* отдают
    # только числа и машинные коды — они в NEVER_LOCALIZED.
    "api_v1_achievements.py",
    "api_v1_dashboard.py",
    # Зал славы: тот же приём, что у api_v1_achievements/api_v1_dashboard —
    # текст (шутка-эквивалент тоннажа, «ещё N тренировок до звания») собран
    # уже готовым внутри use_lang(users.lang), собственных строк модуль не
    # держит, всё через formatting.*/i18n.t.
    "api_v1_hall_of_fame.py",
    # Экран прогресса упражнения: подпись метрики и строка дельты («e1RM: ↑12.5кг
    # с первой тренировки») собираются через i18n.t внутри use_lang(users.lang).
    "api_v1_progress.py",
    # Сбор графика прогресса для обоих потребителей — бота и /v1. Своих
    # литералов не держит вовсе: отдаёт сессии и точки, текст собирает тот, кто
    # показывает.
    "progress_data.py",
    "progression_data.py",
    # Опросник тренера перед сборкой программы — общая для бота и REST `/v1`
    # логика (вопрос о цели, потолок кругов, сборка ответов в одно сообщение
    # модели): все подписи идут через i18n.t внутри use_lang, своих литералов
    # (кроме сравнения с текстом самой модели в SETUP_GOAL_MARKERS) нет.
    "ai_setup_flow.py",
    # Также api_v1_ai.py — ответ модели уже на языке пользователя, а подписи
    # кнопок (label черновика программы, skip_label опросника) идут тем же
    # i18n.t внутри use_lang(users.lang), что и здесь.
    "api_v1_ai.py",
    # Картинка-визитка тренировки для бота и /v1 (api_v1_history.py): своих
    # литералов не держит — подписи собирает formatting.build_workout_card под
    # i18n.use_lang(users.lang), этот модуль только зовёт её и рисует растр.
    "workout_card.py",
    # Экспорт CSV для бота и /v1: заголовки колонок машинные, кириллицы не
    # держит вовсе (см. NEVER_LOCALIZED у api_v1_import.py — тот же случай
    # с другой стороны, импорт вместо экспорта).
    "csv_export.py",
    # Сбор сводки для обоих потребителей — бота и /dashboard. Своих литералов
    # не держит: весь текст собирают formatting.menu_* по текущему языку.
    "dashboard_data.py",
    # Сбор зала славы для обоих потребителей — бота (handlers/history.py) и
    # /v1 (api_v1_hall_of_fame.py). Своих литералов не держит: текст собирают
    # formatting.build_hall_of_fame/format_tonnage_equivalent по текущему языку.
    "hall_of_fame_data.py",
    # Поиск по истории упражнения для обоих потребителей — бота
    # (handlers/history.py.hist_search) и /v1 (api_v1.search_workouts). Своих
    # литералов не держит: это db-запрос плюс постраничная арифметика, текст
    # собирает тот, кто показывает (i18n.t в handlers/history.py, голые числа
    # в JSON).
    "history_search_data.py",
    "keyboards.py",
    "handlers/workout.py",
    "handlers/edit_workout.py",
    "handlers/bodyweight.py",
    "handlers/history.py",
    "handlers/food_diary.py",
    "handlers/csv_import.py",
    "handlers/sharing.py",
    "handlers/exercises.py",
    "handlers/routines.py",
    "handlers/exercise_resolve.py",
    "main.py",
    "ai_limits.py",
    "handlers/settings.py",
    "handlers/fallback.py",
    "handlers/persistent_menu.py",
    "handlers/feedback.py",
    "handlers/donate.py",
    "handlers/community.py",
    "handlers/game.py",
    "handlers/backfill.py",
    "handlers/mcp_access.py",
    "handlers/ios_link.py",
    "search_terms.py",
    "exercise_mentions.py",
    "program_mentions.py",
    "handlers/ai_trainer.py",
    "handlers/factcheck.py",
    "running_texts.py",
]

# Литералы, которым храповик НАМЕРЕННО разрешает кириллицу внутри уже
# LOCALIZED-модуля, — не долг и не забытый перевод, у каждого своя причина.
# Без этого списка единственным способом внести настоящее исключение было бы
# городить его прямо в cyrillic_literals() для всего реестра сразу.
ALLOWED_CYRILLIC: dict[str, set[str]] = {
    # Автоним языка в переключателе (keyboards.LANG_NAMES) — человек, попавший
    # не на тот язык, ищет глазами родное слово, а не перевод (см.
    # TONE_OF_VOICE.md, English voice). Тот же автоним разрешён в en.json
    # тестом test_en_catalog_has_no_cyrillic (_AUTONYM_WHITELIST).
    "keyboards.py": {"Русский"},
    # Канонический ключ группы мышц «Другое» (см. seed_data.py: {"Другое": "other"})
    # — то же самое сравнение с хранимым в БД именем, что formatting.py уже
    # делает в VOLUME_HIDDEN_GROUPS. Группы хранятся канонической русской
    # строкой независимо от языка пользователя и на экране резолвятся через
    # каталог отдельно — это сравнение с данными, а не текст, который видит
    # атлет.
    "handlers/workout.py": {"другое"},
    # Разбор входных дат (Hevy пишет месяц-аббревиатуру на языке телефона
    # пользователя, не только по-английски) — понимать оба языка сразу это и
    # должно, см. комментарий у _MONTH_ABBR_RU/_MONTH_ABBR_EN и _HEVY_DATE_RE
    # в handlers/csv_import.py. Тот же случай, что «сама кириллица в разборе
    # ввода» из шапки этого файла.
    "handlers/csv_import.py": {
        "янв", "фев", "мар", "апр", "май", "июн",
        "июл", "авг", "сен", "окт", "ноя", "дек",
        r"^(?P<d>\d{1,2}) (?P<mon>[A-Za-zА-Яа-яЁё]{3})\.? (?P<y>\d{4})(?:,\s*\d{1,2}:\d{2})?$",
        # SYNONYMS — заголовки колонок своего же экспорта ("дата", "упражнение",
        # "вес", "повторы", "подход", "раунд", "рпе"), по которым автоопределяется
        # маппинг файла. Разбор чужого файла обязан узнавать оба языка сразу,
        # это не текст, который бот показывает атлету.
        "дата", "упражнение", "вес", "повторы", "подход", "раунд", "рпе",
    },
    # FALLBACK_GROUP_NAME в handlers/sharing.py — тот же канонический ключ
    # группы мышц «Другое», что и "другое" у handlers/workout.py выше:
    # сравнение/подстановка данных БД (группы хранятся канонической русской
    # строкой для всех языков), а не текст, который видит атлет.
    "handlers/sharing.py": {"Другое"},
    # _BULK_GROUP_NAME в handlers/exercise_resolve.py — тот же канонический
    # ключ группы «Другое», куда «➕ Создать всё» валит нераспознанные имена
    # оптом: сравнение с именем группы в БД, а не текст на экране.
    "handlers/exercise_resolve.py": {"Другое"},
    # main.py: захардкоженный fallback последнего рубежа (см. _FALLBACK_ERROR_TEXT/
    # _FALLBACK_MENU_BUTTON_TEXT в _generic_error_text/_back_to_menu_markup) — на
    # случай, если сам i18n.t() бросит исключение внутри обработчика ошибок; это
    # постоянный запасной текст, а не забытый перевод. Дальше — dev-логи
    # (logger.exception/logger.error, аудитория — разработчик, не атлет) и
    # русский список админских команд (BotCommand для ADMIN_ID, та же причина,
    # что и у handlers/admin.py в NEVER_LOCALIZED).
    "main.py": {
        "⚠️ Что-то пошло не так — бывает даже у чемпионов. Жми «Меню» внизу — и погнали дальше.",
        "🏠 Меню",
        "Сообщить о проблеме",
        "i18n.t упал внутри обработчика ошибок — показываю запасной текст без него",
        "i18n.t упал при подписи кнопки последнего рубежа — беру запасную подпись",
        "i18n.t упал при подписи кнопки отзыва последнего рубежа — беру запасную подпись",
        "Фоновая задача %s умерла",
        "SetUserLanguageMiddleware: не удалось прочитать пользователя %s",
        "Список пользователей (админ)",
        "Диалоги с AI-тренером (админ)",
        "Лог отправленных пушей (админ)",
        "Что делают пользователи (админ)",
        "Воронка по источникам (админ)",
        "Рассылка всем пользователям (админ)",
        "Релизный анонс: проверить и разослать (админ)",
        "Снести TEST_USER_ID для проверки онбординга (админ)",
    },
    # ai_setup_flow.py: SETUP_GOAL_MARKERS — сравнение с ВОПРОСОМ САМОЙ МОДЕЛИ
    # (узнать, не спросила ли она уже цель тренировок сама), а не текст экрана;
    # тот же приём, что и с разбором ввода выше.
    "ai_setup_flow.py": {
        "цел", "чего хочешь", "чего ждёшь", "зачем тренир", "какой результат",
    },
    # ai_limits.py: preview_text теперь рендерится через i18n.t (см.
    # locales/*.json, ключи limit.preview.*) — экран-предупреждение своим
    # аккаунтам (ADMIN_ID/TEST_USER_ID) показывается на языке ТЕКУЩЕГО
    # запроса, а не жёстко по-русски. Остались только строки `Block.log`/
    # сообщения logger — для разработчика в логе, атлет их не видит ни при
    # каких обстоятельствах.
    "ai_limits.py": {
        " из ",
        " за сутки",
        ": за сутки ~$",
        "AI hard stop: за сутки ~$%.2f, тренер молчит до полуночи UTC",
        "AI hard stop: за сутки ~$%.2f, платный шаг без личной квоты пропущен",
        "не смог посчитать расход за сутки — потолок сегодня не держит",
        "limit %s пропущен: свой аккаунт %s уже нажал «Понятно»",
    },
    # handlers/settings.py: PROFILE_EDIT_FRAME — инструкция самой AI-модели,
    # человек её не видит (см. комментарий над PROFILE_EDIT_FRAME в файле и
    # SETUP_ANSWERS_FRAME/SETUP_ENOUGH_FRAME в handlers/ai_trainer.py — тот же
    # приём там, где модуль ещё не в этом реестре).
    "handlers/settings.py": {
        "Человек пишет это, глядя на экран «Что я про тебя знаю» — то есть правит "
        "твою память о себе, а не спрашивает про тренировки. Что просит убрать — "
        "стирай через save_athlete_profile с forget, что просит поменять — пиши "
        "туда же новым значением. Потом одной строкой скажи, что стало с профилем.\n\n"
        "Его слова: "
    },
    # handlers/feedback.py: служебное уведомление админу о новом отзыве —
    # аудитория один человек (ADMIN_ID), остаётся по-русски независимо от
    # языка отправителя, та же причина, что и у handlers/admin.py.
    "handlers/feedback.py": {"📬 Фидбек от "},
    # handlers/donate.py: то же самое для уведомления админу о донате.
    "handlers/donate.py": {"❤️ Донат: "},
    # search_terms.py: разбор ввода, не показываемый текст (см. CLAUDE.md,
    # Ratchet, и шапку самого модуля). Три группы литералов:
    #  - _CYRILLIC_RE — регексный класс символов, а не слово;
    #  - _NOUN_SUFFIXES/_ENDINGS — русские окончания для стемминга;
    #  - _SYNONYMS/_PHRASES — ключи и значения таблиц синонимов/фраз (основы
    #    слов и словосочетаний, по которым ищутся совпадения в каталоге).
    # Список — точный снимок на момент переноса в LOCALIZED: новая кириллица
    # здесь означает либо ещё один законный элемент разбора (добавь его сюда
    # с той же причиной), либо настоящую утечку — тест обязан её поймать.
    "search_terms.py": {
        "[а-яё]",
        "а", "ам", "ами", "ани", "ание", "анием", "ании", "аний", "анию", "ания",
        "аниям", "аниями", "аниях", "ах", "ая", "е", "ев", "его", "ее", "ей",
        "ем", "ему", "ени", "ение", "ением", "ении", "ений", "ению", "ения",
        "ениям", "ениями", "ениях", "и", "ие", "ии", "ий", "им", "ими", "их",
        "ию", "ия", "иях", "й", "о", "ов", "ого", "ое", "ой", "ом", "ому", "у",
        "ы", "ые", "ый", "ым", "ыми", "ых", "ыях", "ь", "ье", "ью", "ья", "э",
        "ю", "я", "ям", "ями", "ях", "яя", "ё",
        "бабочк", "бедр", "бицепс бедра", "блок", "брус", "выпад", "гантел",
        "гиперэкстенз", "голен", "жим", "жим ногами", "жимовой",
        "задняя поверхность бедра", "запяст", "икр", "качел", "квадрицепс",
        "кор", "кроссовер", "мах", "мостик", "низ спины", "носк", "отжиман",
        "пек", "планк", "подтягиван", "подъем ног", "поясниц", "предплеч",
        "пресс", "присед", "развед", "разведен", "разводк", "разгибание ног",
        "румынск", "свед", "сведен", "сгибание ног", "скручиван", "смит",
        "трапец", "турник", "тяга на прямых", "шраг", "штанг", "ягодиц",
        "ягодичн",
    },
    # exercise_mentions.py: тот же принцип — regex-класс символов и одно
    # каноническое слово-идиома («планка» ловит собственный омоним из прозы
    # тренера, см. _AMBIGUOUS_SINGLE_WORDS в файле), а не текст, который
    # показывается атлету.
    "exercise_mentions.py": {"[а-яa-z0-9]+", "е", "ё", "планка"},
    # program_mentions.py: _TOO_COMMON — фильтр слишком общих слов, которыми
    # не может состоять целиком имя программы (см. шапку файла), тоже разбор,
    # а не показ.
    "program_mentions.py": {
        "день", "дни", "прога", "программа", "программе", "программы",
        "тренировка", "тренировки",
    },
    # exercise_descriptions.py: ТОЛЬКО ключи (канонические русские имена
    # шаблонов — идентификаторы, см. CLAUDE.md, Ratchet), derived from the
    # dicts themselves rather than hand-copied so a new template's key never
    # needs a second, easy-to-forget edit here. This does NOT clear the
    # module for LOCALIZED: EXERCISE_DESCRIPTIONS's ru VALUES are real shown
    # text for ru users (not identifiers, not a translation debt — they're
    # simply the ru half of a bilingual dict), so the module stays in TODO;
    # this entry only keeps report()'s per-module count honest (identifiers
    # excluded, only the still-Russian description text counted).
    "exercise_descriptions.py": set(exercise_descriptions.EXERCISE_DESCRIPTIONS)
    | set(exercise_descriptions.EXERCISE_DESCRIPTIONS_EN),
    # handlers/ai_trainer.py: SETUP_GOAL_MARKERS — по этим кускам узнаём вопрос
    # про цель, который модель задала сама (промпт запрещает, но запрет — не
    # гарантия), чтобы не подставлять свой поверх и не спрашивать дважды. Это
    # сравнение с текстом МОДЕЛИ, а не экран: ответ модели может быть на любом
    # языке (см. ai.language_tail), поэтому список смешивает русские и
    # английские маркеры в одном кортеже — тот же приём, что и у
    # running_texts._TOPIC_STEMS чуть ниже.
    "handlers/ai_trainer.py": {
        "цел", "чего хочешь", "чего ждёшь", "зачем тренир", "какой результат",
    },
    # running_texts.py: _TOPIC_STEMS — ключевые слова-стемы, по которым
    # угадывается тема вопроса для выбора пула placeholder-фраз (см. модульный
    # докстринг): это классификация ВВОДА пользователя, а не текст экрана,
    # ровно тот же случай, что и у search_terms.py/exercise_mentions.py выше.
    # _WORD_RE — regex-класс символов, а не слово; "ё"/"е" — нормализация
    # раскладки в _tokens.
    "running_texts.py": {
        "[а-яa-z0-9]+", "ё", "е",
        "оземпик", "семаглутид", "тирзепатид", "мунджаро", "лираглутид",
        "стероид", "тестостерон", "анабол", "аас", "сарм", "гормон",
        "тренболон", "станозолол", "нандролон", "фарм", "инсулин",
        "боли", "больно", "болит", "болел", "болев", "травм", "растяжен",
        "потян", "восстановлен", "перетрен", "устал", "надорв",
        "сон", "сна", "спать", "спл", "выспа", "недосып", "бессонниц", "циркадн",
        "креатин", "протеин", "гейнер", "бцаа", "добавк", "витамин",
        "омега", "предтрен", "изолят", "казеин", "цитруллин", "бета-алан",
        "питан", "калори", "белк", "углевод", "рацион", "диет", "бжу",
        "разминк", "разминат", "разогрев", "растяжк", "растягив", "заминк",
        "мобильн", "миофасц",
        "заменит", "замена", "заменять", "вместо", "дома", "домашн",
        "инвентар", "оборудован", "гантел", "резинк", "турник", "тренажер",
        "сушк", "похуд", "взвеш", "набира", "масс", "вешу",
        "программ", "сплит", "мезоцикл", "макроцикл", "периодизац",
        "объем", "перегруж", "недел", "баланс", "равномер",
        "прогресс", "рекорд", "плато", "максимум", "вырос", "увелич", "1пм",
        "техник", "правильн", "ошибк", "выполня",
        "истори", "статистик", "раньше", "прошл",
        "сегодня", "сейчас", "щас",
        "мотивац", "лень", "вдохнов", "смысл", "надоел",
    },
}


# --- NEVER_LOCALIZED ---------------------------------------------------------
#
# Модули, у которых кириллица в коде — не долг, а осознанное решение навсегда,
# у каждого — своя причина. Тест никак их не проверяет (ни на кириллицу, ни на
# что-либо ещё) — они просто выведены из-под локализации.
NEVER_LOCALIZED: dict[str, str] = {
    # Админские экраны и инструменты — аудитория один человек (ADMIN_ID),
    # переводить их означает переводить самому себе.
    "handlers/admin.py": "админ-панель бота, аудитория — один человек (ADMIN_ID)",
    "admin_tasks.py": "ежедневная админ-задача (статистика + бэкап), уходит только ADMIN_ID",
    "acquisition.py": "разбор источников трафика и воронка /growth — админская аналитика",
    "announcements.py": (
        "инструмент релизных рассылок: экран подтверждения видит только админ, "
        "а сам текст анонса — свободный ввод админа на каждый релиз, не литерал каталога"
    ),
    "activity_log.py": (
        "лог действий пользователя для админской аналитики (кто как жмёт кнопки), "
        "сами подписи не показываются атлету"
    ),
    # Модули без единого пользовательского текста: чистая логика, структуры
    # данных или инфраструктура — им либо нечего переводить, либо то, что в
    # них есть по кириллице, до Telegram-чата пользователя не долетает.
    "achievement_sync.py": "синхронизация ачивок с БД, не формирует текст",
    "account_deletion.py": (
        "снос аккаунта: своего текста наружу не отдаёт вообще — вызывающие "
        "(handlers/settings.py, api_v1_account.py) говорят с человеком сами, "
        "через i18n; здесь только сообщения logger для разработчика"
    ),
    "ai_program_actions.py": (
        "сохранение черновика программы тренера в БД (общее для бота и REST "
        "`/v1`) — только запись дней/упражнений и машинные коды исхода "
        "(budget/name_conflict), текст под них рисует вызывающий экран"
    ),
    "workout_edit_data.py": (
        "общий хвост правки завершённой тренировки для бота и /v1 — только вызовы "
        "db.*/achievement_sync.resync, своего текста не формирует"
    ),
    "chat_bottom.py": "учёт последнего сообщения в чате, не формирует текст",
    "busy_lock.py": (
        "общий примитив check-and-reserve для busy-замков (бот и REST `/v1`) — "
        "чистая логика над set[int], своего текста наружу не отдаёт вообще"
    ),
    "config.py": "загрузка/валидация конфигурации при старте — лог для деплоя, не для чата",
    "fsm.py": "перечисление FSM-состояний (StatesGroup), текста не содержит",
    "fsm_storage.py": "движок хранения FSM-состояния на диске, не формирует текст",
    "state_scaffold.py": "сброс каркаса FSM между разделами, не формирует текст",
    "timeutil.py": "арифметика дат/времени в часовом поясе пользователя, не формирует текст",
    "i18n.py": (
        "сам модуль локализации: кириллица в нём — только внутренние WARNING/"
        "исключения для разработчика (битый ключ каталога, битый ICU), пользователь их не видит"
    ),
    "push_ios.py": (
        "формирует (title, body) для APNs из готовых строк каталога (push.ios.*) — "
        "единственная кириллица в самом модуле это KeyError для разработчика, "
        "когда для категории не завели короткий текст (тот же случай, что i18n.py выше); "
        "показываемый пользователю текст целиком живёт в locales/*.json"
    ),
    "handlers/__init__.py": "пустой пакетный файл",
    # Сам реестр — служебный инструмент для разработчика, а не экран бота.
    "i18n_coverage.py": "реестр локализации — служебный инструмент, не показывается пользователю",
    "mcp_server.py": (
        "описания инструментов MCP одноязычно английские: один экземпляр сервера "
        "на процесс, схема забирается один раз, request-scoped языка нет"
    ),
    "api_v1.py": (
        "REST-слой для iOS-клиента отдаёт только JSON — числа, id, машинные коды "
        "ошибок (invalid_code, not_found); человеческий текст под них рисует сам "
        "клиент на своей стороне, сюда он не долетает"
    ),
    "api_v1_common.py": (
        "общие помощники REST-слоя: коды ошибок машинные (bad_request, "
        "unauthorized), текст под них рисует клиент"
    ),
    "api_v1_programs.py": (
        "REST-слой программ: JSON с id, именами самих программ (их ввёл "
        "пользователь) и машинными кодами ошибок — своего текста не имеет"
    ),
    "api_v1_food.py": (
        "REST-слой дневника еды: числа, даты и машинные коды ошибок; названия "
        "блюд приходят от пользователя или от модели, а не из каталога строк"
    ),
    "api_v1_media.py": (
        "REST-раздача демонстраций упражнений: ссылки на файлы и машинные "
        "коды ошибок; текст описания приходит из exercise_descriptions уже "
        "на языке пользователя, своих строк модуль не имеет"
    ),
    "api_v1_feedback.py": (
        "REST-приём отзыва и разбора поста: наружу уходят машинные коды, а "
        "текст отзыва и разбора — данные пользователя и ответ модели, не "
        "каталожные строки"
    ),
    "api_v1_import.py": (
        "REST-импорт CSV: числа, машинные коды и английский текст ошибки "
        "разбора с номером строки — русского пользовательского текста нет"
    ),
    "api_v1_account.py": (
        "REST-слой настроек и правки тренировок: значения настроек машинные "
        "(kg/lb, ru/en, epley/brzycki), подписи к ним рисует клиент"
    ),
    "apple_signin.py": (
        "верификация JWT от Sign In with Apple — только английские сообщения "
        "исключений для разработчика/логов, не пользовательский текст"
    ),
    "apns.py": (
        "отправка APNs: только английские сообщения исключений и логов для "
        "разработчика (см. apple_signin.py выше) — сам текст пуша приходит "
        "готовым (title, body) от push_ios.py, этот модуль его не формирует"
    ),
    "api_v1_activity.py": (
        "лог действий из iOS-приложения для админской ленты /activity: фразы "
        "(«записал подход») читает один человек — админ, тот же случай, что у "
        "activity_log.py выше; атлету они не показываются"
    ),
    "api_v1_sharing.py": (
        "REST-слой шаринга программ/упражнений: JSON со снапшотом (имена, ввёл "
        "пользователь) и машинными кодами ошибок — своего текста не имеет, "
        "превью рисует клиент"
    ),
    "api_v1_history.py": (
        "REST-слой истории: PNG-карточка (текст внутри неё собирает "
        "workout_card.py под i18n.use_lang), календарь машинными датами/id и "
        "CSV с латинскими заголовками колонок — своего текста модуль не имеет"
    ),
    "api_v1_templates.py": (
        "REST-слой каталога упражнений: имена шаблонов и описания приходят "
        "уже локализованными от seed_data/exercise_descriptions под "
        "i18n.use_lang, как и /programs/catalog в api_v1_programs.py — своего "
        "текста модуль не заводит"
    ),
    "api_v1_voice.py": (
        "общая расшифровка голоса для /ai/voice и /workouts/.../sets/voice: "
        "тексты ошибок получает уже готовыми строками от вызывающих "
        "api_v1_ai.py/api_v1.py (те зовут i18n.t под своим i18n.use_lang), "
        "своих ключей локализации не заводит"
    ),
}


# --- TODO ---------------------------------------------------------------
#
# Всё остальное: реально пользовательский текст, который ещё не переведён.
# Список существует, чтобы новый файл нельзя было завести молча — он обязан
# попасть либо сюда, либо (после решения) в LOCALIZED/NEVER_LOCALIZED.
# Дальнейшая сортировка внутри TODO не нужна — это рабочий список, а не
# приоритет; приоритет перевода — отдельный разговор.
TODO: list[str] = [
    "achievements.py",
    "ai_trainer.py",
    "analytics.py",
    "bot_profile.py",
    "charts.py",
    "db.py",
    "engagement.py",
    "exercise_descriptions.py",
    "exercise_media.py",
    "formatting.py",
    "game_server.py",
    "mcp_oauth.py",
    "parser.py",
    "progress_ui.py",
    "push_texts.py",
    "seed_data.py",
    "ui.py",
    "video_analysis.py",
    "view_builder.py",
    "voice_parse.py",
]


# --- подсчёт кириллических литералов (как scripts/i18n_extract.py) ----------
#
# Своя маленькая копия, а не импорт scripts.i18n_extract: тот модуль — CLI-
# утилита с argparse в module-level `main()`, тянуть её ради двух функций
# лишний импорт, который к тому же зависит от scripts/ (вне ROOTS реестра).

_CYRILLIC = set("АБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯабвгдеёжзийклмнопрстуфхцчшщъыьэюя")


def has_cyrillic(text: str) -> bool:
    return any(ch in _CYRILLIC for ch in text)


def _docstring_ids(tree: ast.AST) -> set[int]:
    ids: set[int] = set()
    owners = (ast.Module, ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)
    for node in ast.walk(tree):
        if isinstance(node, owners):
            body = getattr(node, "body", None)
            if not body:
                continue
            first = body[0]
            if isinstance(first, ast.Expr) and isinstance(first.value, ast.Constant) and isinstance(
                first.value.value, str
            ):
                ids.add(id(first.value))
    return ids


def cyrillic_literals(module_rel_path: str) -> list[tuple[int, str]]:
    """(номер строки, литерал) для кириллических строковых констант модуля,
    докстринги пропущены. f-строки берутся целиком по своим Constant-кускам
    (совпадает с тем, что реально попадёт в скомпилированный вывод).

    Литералы из ALLOWED_CYRILLIC[module_rel_path] (см. выше — автоним «Русский»
    и подобные намеренные исключения) в выдачу не попадают: это не долг перед
    переводом, а решение навсегда."""
    allowed = ALLOWED_CYRILLIC.get(module_rel_path, set())
    source = (_ROOT / module_rel_path).read_text(encoding="utf-8")
    tree = ast.parse(source, filename=module_rel_path)
    doc_ids = _docstring_ids(tree)
    found: list[tuple[int, str]] = []
    for node in ast.walk(tree):
        if isinstance(node, ast.JoinedStr):
            for part in node.values:
                if (
                    isinstance(part, ast.Constant)
                    and isinstance(part.value, str)
                    and has_cyrillic(part.value)
                    and part.value not in allowed
                ):
                    found.append((node.lineno, part.value))
            continue
        if (
            isinstance(node, ast.Constant)
            and isinstance(node.value, str)
            and id(node) not in doc_ids
            and has_cyrillic(node.value)
            and node.value not in allowed
        ):
            found.append((node.lineno, node.value))
    return found


def report() -> None:
    """Счётчик прогресса локализации: сколько модулей переведено/в очереди, и
    сколько кириллических литералов ещё осталось всего (по модулям из TODO —
    в LOCALIZED их по определению 0, а NEVER_LOCALIZED не считаем, это не долг)."""
    all_modules = set(discover_modules())
    classified = set(LOCALIZED) | set(NEVER_LOCALIZED) | set(TODO)
    unclassified = sorted(all_modules - classified)
    stray = sorted(classified - all_modules)  # в списках, но файла уже нет

    total_leftover = 0
    per_module: list[tuple[str, int]] = []
    for mod in sorted(TODO):
        if mod not in all_modules:
            continue
        n = len(cyrillic_literals(mod))
        total_leftover += n
        per_module.append((mod, n))

    print("=== i18n coverage ===")
    print(f"LOCALIZED:      {len(LOCALIZED)}")
    print(f"NEVER_LOCALIZED:{len(NEVER_LOCALIZED):>4}")
    print(f"TODO:           {len(TODO)}")
    print(f"кириллических литералов осталось (в TODO-модулях): {total_leftover}")
    if unclassified:
        print(f"\nНЕ КЛАССИФИЦИРОВАНЫ ({len(unclassified)}) — храповик должен был это поймать:")
        for m in unclassified:
            print(f"  {m}")
    if stray:
        print(f"\nВ СПИСКАХ, НО ФАЙЛА НЕТ ({len(stray)}) — подчистить реестр:")
        for m in stray:
            print(f"  {m}")
    print("\nТоп-10 модулей TODO по числу кириллических литералов:")
    for mod, n in sorted(per_module, key=lambda x: -x[1])[:10]:
        print(f"  {n:5d}  {mod}")


if __name__ == "__main__":
    report()
