# Статья на vc.ru — готовый текст

Перед публикацией:

- подставить числа вместо `{...}` — они видны в `/check_users` и `/growth`;
- вставить скриншоты в отмеченные места;
- проверить, что все ссылки на бота идут с меткой `?start=src_vc_article`;
- перечитать по `TONE_OF_VOICE.md` цитаты бота: в них говорит тренер, а не я.

Раздел: «Разработка». Альтернативные заголовки — в конце файла.

---

## Формула, которая сделала меня сильнейшим человеком в зале

**Как я написал телеграм-бота для дневника тренировок и нашёл в нём рекорд
720 кг в жиме лёжа**

---

Однажды бот сказал мне, что мой расчётный максимум в жиме лёжа — 720 килограммов.

Это был не сбой отображения и не лишний ноль во вводе. Бот честно посчитал по
формуле, которой пользуется половина фитнес-приложений мира. Формула не врала —
я просто использовал её не там, где она работает. Но чтобы дойти до этой
строчки кода, надо начать с того, зачем всё это вообще писалось.

_[скриншот: карточка тренировки с абсурдным e1RM — если сохранился, иначе
восстановить на тестовой базе]_

### Полторы минуты между подходами

Между подходами есть примерно полторы минуты. За это время нужно записать, что
ты только что сделал, отдышаться и не потерять счёт. Всё, что я пробовал,
съедало эти полторы минуты целиком.

**Заметки в телефоне.** Пишешь как попало, потому что руки в магнезии и ты
торопишься. Через месяц открываешь и видишь `жим 100 8 8 7 92,5 8`. Что здесь
подходы, а что вес? Тот же это жим, что в марте, или другой? Посчитать тоннаж
за месяц — только вручную, с калькулятором.

**Приложения из сторов.** Выбрать упражнение из списка. Выбрать вес колёсиком.
Выбрать повторы колёсиком. Нажать «сохранить». Четыре тапа и три экрана на один
подход — и это на тех, где нет баннера с протеином поверх кнопки. Плюс
подписка, плюс регистрация, плюс онбординг из восьми экранов, где у тебя
спрашивают цель, будто она может быть не «стать сильнее».

**Бумажный блокнот.** Честно лучший из трёх. Пока не забудешь его дома, не
зальёшь водой из шейкера или не потеряешь вместе с двумя годами истории.

Мне хотелось одного: открыть то, что и так открыто — телеграм, — написать одну
строку и закрыть. Так появился бот.

### `100 8`

Это весь интерфейс. Сто килограммов, восемь повторов.

```
100 8        → 100 кг × 8 повторов
100 8 3      → сразу три таких подхода
8            → только повторы, вес возьмётся с прошлого подхода
100 8, 100 7, 95 8   → три подхода одним сообщением
-            → удалить последний
=            → повторить последний
100x8@9      → с RPE, если ведёшь по субъективной сложности
!болит плечо → заметка к упражнению
```

Или голосом: «сто на восемь» — транскрибируется и уходит в тот же парсер.

_[скриншот: ввод подхода — сообщение «100 8» и ответ бота]_

Всё остальное бот считает сам: тоннаж, расчётный максимум в каждом упражнении,
рекорды, серии, звания. Показывает прогресс графиком — по картинке сразу видно,
растёшь ты или третий месяц жмёшь одно и то же, просто в разном порядке.

_[скриншот: экран прогресса с графиком]_

Ещё внутри AI-тренер: он видит твою историю тренировок, а не рассуждает в
вакууме, собирает программу под твой зал и опыт, отвечает про технику и вес.
Дневник еды, вес тела, история за всё время, экспорт — на месте. Бесплатно, без
подписки и без баннеров.

Дальше — три места, где всё оказалось сложнее, чем я думал, когда садился
писать «простой бот на вечер».

### 1. Парсер, который переписывался пятнадцать раз

Казалось бы: `split()` по пробелу, `int()`, готово. На практике `parser.py` —
самый переписываемый файл в проекте, потому что живой человек между подходами
пишет не то, что ты ожидал:

- `100х8` — русская «х» вместо латинской `x`. Пишут так примерно все.
- `100*8`, `100/8`, `100 на 8`, `100-8`.
- `+20 8` — подтягивания с довеском двадцать килограммов. Или это вес снаряда?
- `100 8 8 7` — три подхода с одним весом, но повторы падают.
- `1000 8` — лишний ноль. Это надо не записать, а переспросить.
- `8` — вес или повторы? Повторы: вес берём с прошлого подхода. Потому что
  человек, который пишет одну цифру, только что сделал подтягивания.
- `2: 100 8` — «поправь второй подход», а не «запиши двести на сто восемь».

Отдельная история — что бот отвечает, когда не понял. Валидатор, который
говорит «неверный формат», посреди тренировки бесполезен: человек стоит с
телефоном в руках и не знает, что делать дальше. У меня записано правило, и на
него есть тесты: **каждая ошибка отвечает на два вопроса — что случилось и что
делать дальше**.

```
Не понял вес. Напиши число, например 80
Не понял, где тут вес и повторы: «100 восемь»
Подозрительно большой вес — лишний ноль?
```

Разница между «неверный формат ввода» и «Напиши число, например 80» — это
разница между человеком, который бросил бота на второй тренировке, и человеком,
который ведёт в нём дневник второй год.

### 2. Формула, из-за которой я стал жать 720 кг

Теперь про рекорд.

Чтобы сравнивать тренировки между собой, нужен один общий знаменатель.
100 кг × 8 и 110 кг × 5 — это примерно одинаковая работа, но по одному весу
этого не видно. Для этого есть e1RM — расчётный максимум: какой вес ты поднял
бы один раз. Считают его по формулам, самые известные — Эпли и Брзицки.

Брзицки точнее в рабочем диапазоне, и я поставил его вариантом на выбор:

```python
weight * 36 / (37 - reps)
```

Посмотрите на знаменатель. При 8 повторах — `37 - 8 = 29`, всё хорошо. При 30
повторах — `37 - 30 = 7`, и формула выдаёт пятикратный вес. При 36 — `37 - 36 = 1`,
то есть **тридцатишестикратный**.

А теперь представьте, что делает человек в конце тренировки. Он берёт двадцать
килограммов и жмёт их в отказ — тридцать шесть раз. Формула честно считает:
20 × 36 = 720 кг. Бот честно записывает это в рекорды. Навсегда. Потому что
рекорд — это максимум за всю историю, и побить 720 кг человек уже не сможет
никогда.

Причём стояла у меня и защита — `if reps >= 37`. Ровно на один повтор правее
пика: при 37 знаменатель обращается в ноль, и я аккуратно закрыл деление на
ноль, не заметив, что вся катастрофа происходит слева от этой границы.

Починилось так: Брзицки работает до 10 повторов, дальше передаём Эпли, который
монотонен и не взрывается никогда. Красота в том, что на 10 повторах формулы
совпадают **точно**: `1 + 10/30 = 36/27 = 4/3`. Не «примерно» — тождественно. То
есть переход между формулами не даёт ступеньки в графике, и человек, который
жмёт то на 8, то на 12, не увидит разрыва в своих цифрах.

Мораль, которую я вынес: в фитнес-приложении баг в формуле не роняет сервис. Он
тихо портит человеку данные за год и делает график бессмысленным — а заметит
это человек, а не мониторинг.

### 3. Тон голоса как инженерная задача

У бота есть характер: тренер из подвальной качалки. Немногословный, на «ты»,
говорит от первого лица — «Записал», «Пересчитал сам». Это не украшательство: в
приложении, куда пишут между подходами, каждое лишнее слово читается штангой в
руках.

Неожиданным оказалось то, что тон приходится **защищать тестами**, как код.
Правил накопилось на отдельный документ, вот главные:

- Страдательный залог запрещён. «Заметка сохранена» — это голос базы данных.
  «Записал заметку» — голос тренера. Если в фразе некому сказать «я», фразу
  писал не тот персонаж.
- Подколки разрешены **только** про пропуски тренировок. За малый вес, за
  лёгкую тренировку, за то, что человек новичок, — никогда. Это, кстати,
  единственное правило, которое я вводил после живой обратной связи: пошутить
  про рабочий вес — самый быстрый способ, чтобы человек больше не вернулся.
- **Пуш обязан быть правдой.** Если бот пишет «до звания — две тренировки», это
  проверено по данным. Если данных на утверждение нет, вариант просто выпадает
  из ротации, а не заполняется догадкой. «Среда — твой продуктивный день»
  отправляется только при явном лидере в статистике.

Последнее оказалось самой дорогой в реализации строчкой из всего документа — и
самой важной. Приложение, которое один раз польстило тебе неправдой, ты потом
не можешь использовать как дневник: цифрам в нём больше нет доверия.

### Немного технического, для тех, кто дочитал

- **Python, aiogram 3, SQLite, деплой на Amvera.** Хостинг — {K} ₽ в месяц,
  AI-запросы — ещё около {L} ₽ при текущем числе людей.
- **SQLite в WAL.** В обычном режиме каждый коммит создаёт и удаляет файл
  журнала, а писатель блокирует читателей — то есть запись подхода
  останавливает все чтения. Замер на 200 коммитов: 3.96 мс → 2.29 мс, минус
  42%, ценой одной строки `PRAGMA journal_mode=WAL`.
- **{T} тестов**, гоняются перед каждым пушем. Звучит как перебор для
  телеграм-бота, но дневник тренировок, который потерял тренировку, не нужен
  никому: это не лента, которую можно перезагрузить, а данные, которые человек
  вводил руками полтора года.
- **Карточку тренировки можно забрать картинкой** — и на ней URL-кнопка, а не
  callback-кнопка. Причина: callback-кнопки не переживают пересылку, а картинку
  как раз уносят в чат с друзьями. Мелочь, которая превращает картинку из
  скриншота в канал привлечения.
- **Дневник подключается к Claude и ChatGPT по MCP.** То есть можно спросить у
  своей любимой модели «посмотри мой жим за полгода и скажи, где я встал» — и
  она посмотрит настоящие данные, а не то, что ты вспомнил.

### Что я понял про людей, а не про код

_[раздел заполнить своими наблюдениями — ниже кандидаты, оставить только те,
которые подтверждаются данными из `/growth` и `/check_users`]_

- Приходят за дневником, остаются из-за графика прогресса. Дневник — это работа,
  график — награда.
- Главная развилка — первая записанная тренировка. Кто записал одну, обычно
  записывает и десятую; кто не записал ни одной в первый день, не вернётся.
  Поэтому в воронке я считаю не установки, а именно первую запись.
- Голосовым вводом пользуются меньше, чем я ожидал. В зале шумно, и говорить
  «сто на восемь» вслух посреди зала стесняются.
- Напоминания работают, только когда они про конкретный пропуск. «Привет, давно
  не заходил» — мимо, «третья пропущенная — серия на кону» — работает.

### Что дальше

_[2–3 пункта планов, без обещаний дат]_

### Ссылка

Бот: **t.me/lightweight_log_bot?start=src_vc_article** — бесплатно, без
подписки и рекламы протеина.

Если попробуете и что-то не так — в боте есть `/feedback`, отзывы приходят мне
лично, отвечаю сам. Расскажите заодно, чем вы ведёте дневник сейчас: мне до сих
пор интересно, сколько людей в 2026 году носят в зал бумажный блокнот.

---

## Альтернативные заголовки

- «Как я нашёл в своём боте рекорд 720 кг в жиме лёжа» (кликабельнее, но
  честнее не выглядит — решай сам)
- «Формула, которой пользуется половина фитнес-приложений, врёт на высоких
  повторах. Разбираю на своём боте»
- Для раздела «Здоровье»: «Как перестать терять прогресс в зале: дневник, в
  который пишешь одну строку между подходами»

## Заготовка первого комментария от автора

vc.ru любит, когда автор сам открывает обсуждение. Кандидат:

> Отдельно интересно собрать коллекцию того, как люди пишут вес и повторы. У
> меня в парсере уже есть `100х8` русской «х», `100 на 8`, `100*8` и `+20 8`.
> Если пишете как-то иначе — скиньте, добавлю поддержку.
