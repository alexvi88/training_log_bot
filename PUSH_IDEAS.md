# Пуши: голос «Привет Атлет»

**Статус: реализовано.** Все 8 категорий ниже — рабочий код, не только идеи:
`push_texts.py` (тексты + ротация), `engagement.py` (сигналы, приоритет, джобы),
хук в `handlers/workout.py` (отложенный пуш после тренировки), админ-команда
`/pushes` (лог отправленного). Выключено по умолчанию флагом `ENGAGEMENT_ENABLED`
(env var) — включить явным решением, когда будете готовы.

Известный пробел: индивидуальные тумблеры категорий в ⚙️ Настройках (правило 5
ниже) пока не реализованы — есть только общий выключатель на уровне деплоя.

Бэклог пуш-уведомлений бота-дневника тренировок в едином голосе: тренер из
подвального зала. Строгий, но свой. Хвалит по делу, подкалывает только за
пропуски, никогда не сюсюкает.

Красивый пример-визуализация: см. опубликованный артефакт «Привет Атлет —
голос пушей» (гайд, таблица триггеров, карточки уведомлений по каждой категории).

## Голос — 5 правил

1. Обращение — «АТЛЕТ» капсом. Никаких «боец», «пользователь» и «дорогой друг».
2. Хвалим конкретно — за цифру, вес, серию. Не общими словами вроде «ты молодец».
3. Подкалываем **только за пропуски**. Никогда — за маленький вес, неудачный подход
   или медленный прогресс.
4. Один пуш в день. Тренер требователен, но не долбит в дверь.
5. После подколки всегда есть выход — рядом кнопка «▶ Начать тренировку».

**Ещё одно правило, важнее остальных: пуш — только для того, чего юзер не видит,
потому что его нет в приложении.** Если сигнал случается в тот же момент, когда
человек и так смотрит на экран (например, при завершении тренировки), это не пуш,
а строка в существующем сообщении.

Поэтому здесь **нет** категорий «Победа» и «Юбилей»: PR уже подсвечивается прямо
в сообщении о завершении тренировки (`formatting.format_pr_detail`,
`handlers/workout.py`), а юбилей (100-я тренировка, год в боте) случается в ту же
секунду — его стоит дописать туда же строкой, а не слать вдогонку то, что человек
только что видел своими глазами.

**Тихие часы:** 23:00–8:00 соблюдает отложенный пуш после тренировки (`engagement.in_quiet_hours`);
ежедневная рассылка и так фиксирована на один час (`ENGAGEMENT_HOUR`, по умолчанию 19:00).

---

## Триггеры и тексты — полная таблица

Один ежедневный джоб (`run_daily_engagement_job`, час задаётся `ENGAGEMENT_HOUR`) проверяет
категории по порядку (колонка «Ранг») и шлёт первую подходящую — не больше одного пуша в день
на человека. Ранг — это порядок проверки, не время отправки: весь джоб срабатывает в один и тот
же час, просто конкретная категория может быть неприменима в этот день (например, «Плато»
проверяется только по воскресеньям). «После тренировки» вне ранга — отдельный, транзакционный
пуш, не конкурирует за дневной слот.

В колонке «Текст пуша» — все варианты ротации для категории (бот показывает их по кругу,
без повтора, пока не пройдёт весь список). Плейсхолдеры вида `{exercise}`/`{tonnage}` в реальной
рассылке подставляются данными; здесь — на примерах.

**Эта таблица сгенерирована из `push_texts.py`.** После правки текстов/триггеров в коде — перегенерировать:

```
python3 scripts/gen_push_table.py
```

Скрипт сам находит границы `<!-- PUSH_TABLE_START/END -->` ниже и переписывает только их,
остальной текст файла не трогает. Таблицу можно и просто редактировать руками — при следующем
запуске скрипт её перезапишет заново из кода, так что ручная правка имеет смысл только как
временная заметка, а не постоянный источник истины.

<!-- PUSH_TABLE_START -->
| Ранг | Категория | Триггер | Вариант | Текст пуша (пример) |
|---|---|---|---|---|
| 1 | **Серия на кону** | Сб/вс, `week_streak >= 2`, тренировок на этой неделе — 0 | 1 | АТЛЕТ, серия на кону. 6 недель подряд, а на этой — пока ноль. До конца недели последний день. Серию строил ты — не разваливай сам. |
|  |  |  | 2 | АТЛЕТ, 6 недель без срывов висят на волоске. Одна тренировка — и цепь держится. Выбор за тобой. |
|  |  |  | 3 | АТЛЕТ, сутки на то, чтобы серия не превратилась в «когда-то тренировался». 6 недель — не шутка. |
| 2 | **Пропуск** | Ровно N дней с последней тренировки, N ∈ {3, 5, 7, 10, 14} | 1 | АТЛЕТ, пару дней тишины. Отдых или отмазки — сам разберёшься. Я просто считаю дни. |
|  |  |  | 2 | АТЛЕТ, второй-третий день без зала. Штанга пока не в обиде. Пока. |
|  |  |  | 3 | АТЛЕТ, пятый день без зала. Мышцы ещё помнят, как работать. Привычка — уже забывает. |
|  |  |  | 4 | АТЛЕТ, четвёртый-шестой день тишины. Самое время это прервать, а не привыкать. |
|  |  |  | 5 | АТЛЕТ, неделя простоя. Штанга не обидится — она железная. А форма обидится, и быстро. |
|  |  |  | 6 | АТЛЕТ, ровно неделя. Не буду читать нотации — просто напомню, где дверь в зал. |
|  |  |  | 7 | АТЛЕТ, десятый день. Это уже не пауза, это привычка не приходить. Ломаем её сегодня? |
|  |  |  | 8 | АТЛЕТ, две недели. Я не читаю нотации, я считаю дни. Зал на месте. Ты — где? |
|  |  |  | 9 | АТЛЕТ, четырнадцать дней тишины. Хватит уже — приходи, разберёмся на месте. |
| 3 | **Возвращение** | `days_since_last >= 21`, затем каждые 10 дней (21, 31, 41…) | 1 | АТЛЕТ, три недели — и это ок. Возвращаться после паузы умеют все. Не оправдывайся — приходи, сегодня полегче, без геройства. |
|  |  |  | 2 | АТЛЕТ, твой прошлый рекорд всё ещё здесь и ждёт, когда ты придёшь его побить. Он терпеливый. |
|  |  |  | 3 | АТЛЕТ, начни с лёгкого. Первый шаг после паузы — самый ценный. Разомнёмся вместе. |
| 4 | **Тайминг** | Сегодня — самый частый день тренировок по истории (нужно ≥10 тренировок), сегодня ещё не тренировался | 1 | АТЛЕТ, обычно в это время ты уже под грифом. Тело помнит расписание. А ты? |
|  |  |  | 2 | АТЛЕТ, твой рабочий день — сегодня. Ты знаешь, что делать. |
|  |  |  | 3 | АТЛЕТ, час до твоего обычного окна. Успей поесть — и вперёд. |
| 5 | **Плато** | Вс: тот же рабочий вес 3 тренировки подряд, каждый раз 12+ повторов | 1 | АТЛЕТ, Жим лёжа третью тренировку на одном весе — и каждый раз выше 12 повторов. Это не плато, это ты придерживаешь вес. Добавь. |
|  |  |  | 2 | АТЛЕТ, Жим лёжа: 12+ повторов три тренировки подряд на одном весе. Тело готово к большему — осталось загрузить. |
|  |  |  | 3 | АТЛЕТ, Жим лёжа явно стал легче — три сессии за 12 повторов, а вес всё тот же. Скромничаешь? |
| 6 | **Аналитика** | Вс, нет активного плато, суммарный тоннаж за 30 дней > 0 | 1 | АТЛЕТ, за месяц ты поднял суммарно 4.2 т — это примерно один синий кит. Зайди, покажу разбивку. |
|  |  |  | 2 | АТЛЕТ, на этой неделе — 2 тренировки. Заходи, гляну, что подросло. |
|  |  |  | 3 | АТЛЕТ, понедельник — твой самый продуктивный день по истории. Держим планку? |
| 7 | **Челлендж** | Пн (старт) или чт при < 2 тренировок за неделю (прогресс) | 1 | АТЛЕТ, задача недели: три тренировки за семь дней. Погнали. |
|  |  |  | 2 | АТЛЕТ, мини-квест: побей любой свой рекорд на этой неделе. Приз — самоуважение. Котируется дорого. |
| — | **После тренировки** | Через 2 ч после завершения живой (не задним числом) тренировки — транзакционное, не конкурирует за дневной слот | 1 | АТЛЕТ, теперь вода, белок и сон — тренировка кончается не в зале, а на кухне. |
|  |  |  | 2 | АТЛЕТ, работа сделана, но не до конца — без нормального восстановления сегодняшний труд наполовину впустую. Попей воды. |
|  |  |  | 3 | АТЛЕТ, плюс один в журнал. Теперь дай телу то, что оно заслужило: еду, воду и сон. |
<!-- PUSH_TABLE_END -->

Каждый сигнал уже есть (или почти есть) в БД:

- `last_finished_workout_dates` — даты завершённых тренировок → дни простоя, стрик
- `week_streak` / `last_30_days` / `this_week` — из `analytics.compute_dashboard`
- история по упражнению + «прошлая сессия» (`prev_sets`) — застой/прогресс по весам
- счётчик тренировок (`count_workouts`), длительность тренировки

---

## Как это устроено в коде

- **`push_texts.py`** — пулы текстов (`TEXTS`), у каждого пользователя своя перемешанная
  «сумка» вариантов на категорию (`push_rotation` в БД), чтобы формулировка не повторялась,
  пока не пройдёт весь пул.
- **`engagement.py`** — чистые функции-детекторы сигналов (легко тестируются без БД:
  `is_streak_at_risk`, `skip_milestone`, `is_win_back_day`, `usual_weekday`, `is_plateau`,
  `format_tonnage`, `in_quiet_hours`) плюс `build_daily_push()`, которая перебирает категории
  по приоритету и отдаёт первую подходящую. Два фоновых джоба — `run_daily_engagement_job`
  (раз в день, час задаётся `ENGAGEMENT_HOUR`) и `run_followup_job` (раз в
  `FOLLOWUP_POLL_MINUTES`, проверяет отложенные пуши после тренировки).
- **Дедуп**: `db.has_push_today` — не больше одного пуша из дневной ротации в календарный
  день на пользователя; лог всех отправок — таблица `pushes`.
- **Хук в `handlers/workout.py`**: по завершении живой (не задним числом) тренировки
  вызывается `db.schedule_followup`, который ставит отложенный пуш через
  `FOLLOWUP_DELAY_HOURS` (по умолчанию 2 часа).
- **Кнопка в пуше** — «▶ Начать тренировку» (`keyboards.push_cta_keyboard`), кроме
  недельной аналитики (там нет прямого действия).
- **`/pushes`** — админ-команда, постранично показывает лог отправленных пушей,
  сортировка по времени, самые новые сверху; кому, какая категория, какой текст.

**Включение:** `ENGAGEMENT_ENABLED=true` в окружении — по умолчанию выключено.
