# UX/UI: где жмёт и что с этим делать

*Июль 2026. Разбор четырёх экранов по скриншотам реального использования + прочёс кода
экранов (`formatting.py`, `keyboards.py`, `handlers/workout.py`). В отличие от
`PRODUCT_ANALYSIS.md` — это не про фичи, а про то, как выглядят и сколько тапов стоят
экраны, которые уже есть.*

**Статус:** часть 1 (§1.4, §1.5, §2.1, §3.1, §3.3, §4.1, §4.5, §4.6) сделана в PR #188.
Часть 2 — разбор остальных экранов и всё, что из него сделано, — в
[`UX_IMPROVEMENTS_PART2.md`](UX_IMPROVEMENTS_PART2.md).

---

## Главное в шести пунктах

1. **Самое частое действие в зале — «тот же вес, ещё сет» — стоит открыть клавиатуру
   и напечатать.** Бот уже посчитал цель (`🎯 Цель: 39.3×8`) и знает последний сет, но ни
   то ни другое не является кнопкой. Обработчик повтора даже написан и висит без кнопки
   (`handlers/workout.py:1444`).
2. **Живой трекер не отвечает на вопрос «где я».** Нет номеров сетов, нет счётчика
   сетов/тоннажа, нет паузы между подходами. Пять строк «• 4×4» подряд глазами не
   считаются.
3. **Карточка завершения раздута в 2 раза лишними строками** — каждый одинаковый сет
   отдельным пунктом, «прошлая» отдельной строкой, `[СПИНА]` в каждом заголовке. На
   скриншоте 6 строк там, где хватает 3.
4. **Эмоциональный пик карточки сразу засыпается меню** с картинкой-теплокартой
   (`_finalize_workout`, `handlers/workout.py:1895`). Юзер смотрит на рекорд полсекунды.
5. **Добавить упражнение = 3 тапа** (группа → список → упражнение), хотя в 80% случаев
   это одно из 3–4 недавних. Ни на экране паузы, ни на экране групп недавних нет.
6. **Мелочи, которые читаются как баги:** кнопка `📝` без подписи, «❌ Отмена» которая
   ничего не отменяет, мёртвый переключатель `show_extra_stats` в БД без UI.

---

## Экран 1. Живой трекер (скрины 2, 4, 5) — 90% времени в зале

### Что сейчас

```
▶ overhead press - machine
  • 4×4
──────────
💡 В прошлый раз: 39.3×7, 36×6
🎯 Цель: 39.3×8

[ ▶ overhead press - machine ]
[ seated row - 1hand - cable ]
[ ↩️ Удалить последний       ]
[ ➕ Суперсет ] [ 📝 ]
[ ✅ Закончить упражнение    ]
```

Пять рядов кнопок + залипшее фото упражнения сверху + постоянное меню снизу. При этом
ни одна кнопка не логирует подход — основное действие возможно только текстом.

### 1.1 Две кнопки, которые логируют сет (главная рекомендация)

```
[ 🎯 39.3×8 ] [ 🔁 36×6 ]
```

- Левая — залогировать цель из `analytics.suggest_progression` (уже считается для
  подсказки `🎯 Цель`, `handlers/workout.py:290`).
- Правая — копия последнего сета, это ровно существующий `live:repeat`
  (`handlers/workout.py:1444`, кнопку срезали в #164 в пользу текстовой «=»).

Обе подписаны тем, что залогируют, — тап не «вслепую». Текстовый ввод остаётся для
всего остального, но два самых частых случая перестают требовать клавиатуру и мел на
руках. Цена — десяток строк в `logging_keyboard` (`keyboards.py:144`).

Риск случайного тапа закрывается тем, что уже есть: тост с тем, что залогировано, и
«↩️ Удалить последний» рядом.

### 1.2 Номера сетов и итог по упражнению

```
▶ overhead press - machine · 3 сета · 1.2 т
  1) 39.3×7
  2) 39.3×8  ✅ цель
  3) 36×6
```

Номер отвечает на «это у меня четвёртый или пятый», галочка на сете, закрывшем цель,
переносит уже существующий флаг `achieved` (`format_progression_hint`) с подсказки на
конкретный сет. Правка — `build_live_session_text` (`formatting.py:460`).

### 1.3 Пауза перед сетом вместо таймера отдыха

`sets.created_at` пишется на каждый сет (`db.py:113`) — разница между сетами доступна
бесплатно:

```
  2) 39.3×8   · 2:10
  3) 36×6     · 3:40
```

Это ретроспектива, а не тикающий таймер: живой отсчёт означал бы редактирование
сообщения раз в секунду — лимиты Telegram этого не переживут, а трекер и так
пересоздаётся при каждом обновлении (`_refresh_live`). Ретроспективная пауза даёт 90%
пользы за 0% риска: юзер видит, что отдыхает по 4 минуты вместо двух.

Если таймер всё-таки нужен — отдельная кнопка «⏱ 2 мин», которая планирует один пуш на
момент окончания отдыха. Это уже средняя по цене задача (нужна отложенная задача +
отмена при следующем сете), и я бы её отложил до появления запроса.

### 1.4 Разгрузить клавиатуру

- `📝` → `📝 Заметка`. Голая пиктограмма рядом с «➕ Суперсет» читается как «черновик»
  или «редактировать» — а это заметка к упражнению.
- Табы суперсета — в один ряд по два, с укороченными названиями
  (`seated row - 1hand - cable` → `seated row…`). Сейчас каждое открытое упражнение
  забирает целый ряд, и на трёх параллельных упражнениях клавиатура становится выше
  самого трекера.
- Названия в табах дублируют текст трекера — там же они видны с ▶. Укорачивание ничего
  не теряет.

### 1.5 Опечатки веса никто не ловит

На скрине 5 в жиме лежит `1×1, 1×1` при прошлых `140×6` — если это набранное `1 1`
вместо `140 6`, то ничто на это не намекнёт, а тоннаж и e1RM за месяц испортятся.
Мягкий вариант: если вес меньше 40% от последнего рабочего по этому упражнению (и не
0 — своим весом это норма), добавить в трекер тихую строку `⚠️ 1кг? в прошлый раз 140`.
Не блокировать, не спрашивать — просто показать.

---

## Экран 2. Пауза между упражнениями (idle)

Сейчас: `▶️ Следующее по шаблону` / `➕ Упражнение` / `⏭ Как в прошлый раз` /
`🏁 Завершить тренировку` (`keyboards.py:175`) плюс подсказка «💡 В прошлый раз дальше
было: X».

### 2.1 Недавние упражнения в один тап

`⏭ Как в прошлый раз` угадывает одно упражнение. Когда не угадало — снова 3 тапа через
группы. Добавить ряд из 2–3 последних использованных упражнений (данные уже есть,
`list_user_exercises` сортирует по недавности):

```
[ ⏭ Как в прошлый раз: leg press ]
[ 🕘 pull down ] [ 🕘 seated row ]
[ ➕ Упражнение ]
[ 🏁 Завершить тренировку ]
```

### 2.2 Сводка сессии — здесь, а не в трекере

Пауза — естественное место для «сколько уже сделано»:

```
🏋️ 47 мин · 4 упражнения · 11 сетов · 5.2 т
```

В самом трекере такой строки лучше не держать: она отъедает высоту у того, для чего
экран нужен (сеты + поле ввода).

---

## Экран 3. Выбор упражнения (скрин 3)

### 3.1 «❌ Отмена» ничего не отменяет

`pick:cancel` возвращает на трекер или на экран паузы (`_back_after_cancel`), тренировка
не страдает. Но красный крестик посреди тренировки читается как «отменить тренировку».
Заменить на `⬅️ Назад`.

### 3.2 Недавние выше групп

Экран групп (`_picker_screen_groups`, `handlers/workout.py:888`) — всегда 6 групп + «Все».
Ряд `🕘 Последние: pull down · leg press · abs` сверху убирает один уровень навигации
для самого частого случая.

### 3.3 Группы — по частоте, а не по алфавиту

`groups_keyboard` (`keyboards.py:57`) отдаёт группы в порядке БД. Сортировка по тому,
что юзер реально тренирует, ставит нужную кнопку первой.

### 3.4 Про поиск текстом уже написано — этого достаточно

Подпись «или найди упражнение по названию» + рабочий `pick_exercise_search` на обоих
экранах. Единственное, чего не хватает — намёка, что искать можно и с экрана групп; сейчас
текст подсказки это уже говорит. Оставить как есть.

---

## Экран 4. Карточка завершённой тренировки (скрин 1)

### Что сейчас

```
conventional deadlift [СПИНА]
  • 190×5
  • 190×5
  • 190×5
  ↳ e1RM 221.7кг (↑+5.7кг vs 22.07)
  [прошлая: 180×6, 180×6, 180×6]
```

Шесть строк на упражнение × 5 упражнений + два разделителя + два свёрнутых блока. На
телефоне это два экрана прокрутки, и `fit_workout_text` уже вынужден срезать хвост
на длинных тренировках («Показано N из M»).

### 4.1 Схлопывать одинаковые сеты

```
  • 190×5 ×3
```

Нотация уже знакома юзеру — ровно так парсер принимает ввод (`100x8x3`, `parser.py`).
Три строки → одна, без потери информации. То же для «прошлой»: `180×6 ×3`.

### 4.2 «Прошлую» — в строку e1RM

```
  ↳ e1RM 221.7кг (↑+5.7 vs 22.07) · было 180×6 ×3
```

Минус строка на упражнение. Итого с 4.1: блок жима на скрине сжимается с 6 строк до 3,
карточка — примерно вдвое, и обрезание хвоста перестаёт срабатывать.

### 4.3 `[СПИНА]` в каждом заголовке

Пять заглавных тегов на карточке — самый заметный визуальный шум, и для человека,
который только что это отжал, информации в них ноль. Распределение по группам живёт в
недельном объёме, а не здесь. Убрать с карточки завершения (в истории и в управлении
упражнениями оставить).

### 4.4 Счётчики в строку тоннажа

Сейчас: `🏋️ Суммарно за тренировку — 11 тонн. Это как 20 коней 🐴`.
Количество упражнений и сетов есть только в PNG-версии карточки
(`build_workout_card`, `formatting.py:398`), в сообщении их нет:

```
🏋️ 5 упражнений · 13 сетов · 11 тонн — это как 20 коней 🐴
```

### 4.5 Не засыпать карточку меню

`_finalize_workout` дорисовывает карточку и **сразу** отправляет отдельное сообщение
меню с PNG-теплокартой (`handlers/workout.py:1895`). Рекорд и комментарий тренера
уезжают вверх, не успев быть прочитанными. Дать карточке кнопку `⬅️ В меню` и не
отправлять меню автоматически — минус одно тяжёлое сообщение в чате и сохранённый пик.

### 4.6 Кнопки карточки

`workout_card_keyboard` (`keyboards.py:381`) — только `🖼 Картинка` и `📝 Заметка`.
Заметить опечатку в сете проще всего именно сейчас, а правка требует
Меню → История → выбрать тренировку. Добавить `✏️ Редактировать` (коллбек уже есть в
`history_item_keyboard`).

### 4.7 Мёртвый переключатель

`users.show_extra_stats` (`db.py:32`) гейтит строку e1RM на карточке, но в
`settings_keyboard` (`keyboards.py:486`) его нет — выключить нельзя. Либо вывести как
«Карточка: подробно / компактно», либо убрать колонку.

---

## Сквозное

### 5.1 Залипшее фото упражнения — самый шумный элемент

`_sync_sticky_photo` (`handlers/workout.py:182`) при каждой смене упражнения отправляет
фото или медиагруппу и удаляет предыдущую. За тренировку из 6 упражнений это 6
отправок и 6 удалений, трекер каждый раз прыгает. Тому, кто знает технику жима, это
чистый шум (и трафик — на скрине видно «320 KB»).

Предложение: показывать автоматически только первые 2–3 раза для нового упражнения,
дальше — кнопкой `ℹ️ Техника`. Порог считается по числу сессий с упражнением, данные
есть. Заодно снимает нагрузку на пересоздание трекера.

### 5.2 Язык кнопок

- `🏋️ НАЧАТЬ ТРЕНИРОВКУ` — единственная капсом кнопка в приложении. Как акцент главного
  действия это работает; оставить, но не размножать.
- Постоянное меню (`Тренировка` / `Меню` / `AI-тренер`) без эмодзи, инлайн-кнопки
  эмодзи-плотные. Не проблема, но если приводить к одному стилю — то в сторону
  постоянного меню, а не наоборот.
- `➕ Суперсет` (открыть второе упражнение параллельно) и `➕ Упражнение` на экране
  паузы — разные вещи с почти одинаковой подписью. Терминология «суперсет» правильная,
  но стоит уточнить: `➕ Суперсет (параллельно)`.

### 5.3 Что уже хорошо и что трогать не надо

- Текстовый ввод сетов с сокращениями `-` / `=` / `!` / `N:` — быстрее любой клавиатуры
  для того, кто их знает. Кнопки из §1.1 их не заменяют, а дублируют для тех, кто не
  знает.
- Скрытие инструкции по вводу для опытных (`analytics.is_seasoned`) — правильная
  прогрессивная выдача.
- 🔥-реакция на сообщение с рекордом вместо ещё одного сообщения.
- Трекер, прижатый к полю ввода, и удаление своих сообщений — ядро всего UX, тут ничего
  не менять.

---

## Приоритизация

| Горизонт | Что | Эффект |
|---|---|---|
| **Часы** | 🎯/🔁 кнопки логирования (1.1) · номера сетов (1.2) · `📝 Заметка` (1.4) · схлопывание одинаковых сетов (4.1) · «прошлая» в строку e1RM (4.2) · счётчики в тоннаж (4.4) · `✏️ Редактировать` на карточке (4.6) · `⬅️ Назад` вместо «Отмена» (3.1) · `show_extra_stats` в настройки (4.7) | Убирает клавиатуру из главного действия и режет карточку вдвое. Всё опирается на уже посчитанное |
| **1–2 дня** | Пауза между сетами (1.3) · недавние в один тап (2.1, 3.2) · сводка на экране паузы (2.2) · политика залипшего фото + `ℹ️ Техника` (5.1) · табы суперсета в один ряд (1.4) · снять `[ГРУППА]` с карточки (4.3) · не отправлять меню поверх карточки (4.5) | Экран перестаёт быть перегруженным, навигация в зале — 1 тап вместо 3 |
| **Позже / по запросу** | Таймер отдыха с пушем (1.3) · предупреждение о подозрительном весе (1.5) · сортировка групп по частоте (3.3) | Полезно, но требует новых механик или может раздражать |

**Рекомендуемый первый заход:** §1.1 + §1.2 + §4.1 + §4.2. Это один вечер работы, и он
закрывает две самые заметные жалобы скриншотов — «логировать сет надо печатать» и
«карточку невозможно охватить взглядом».

### Рассмотрено и отклонено

- **Цифровая клавиатура для веса/повторов инлайном.** 4–5 рядов кнопок ради того, что
  печатается за 4 символа; трекер уедет за пределы экрана.
- **Кнопки `−2.5` / `+2.5` к весу.** Шаг веса зашит константой и часто неверен для
  гантелей и блоков (см. `_WEIGHT_STEP`, `handlers/workout.py:261`) — кнопка будет
  предлагать несуществующий вес. Сначала шаг на упражнение, потом кнопки.
- **Живой тикающий таймер отдыха.** Редактирование сообщения раз в секунду против
  лимитов Telegram.
- **Mini App для логирования.** Ломает главное преимущество — лог из чата без выхода в
  приложение.
