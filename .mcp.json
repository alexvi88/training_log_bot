{
  "mcpServers": {
    "training-log": {
      "type": "http",
      "url": "${TRAINING_LOG_MCP_URL:-https://training-log-bot-vialex8.waw0.amvera.tech/mcp}",
      "headers": {
        "Authorization": "Bearer ${TRAINING_LOG_MCP_TOKEN}"
      }
    }
  }
}
